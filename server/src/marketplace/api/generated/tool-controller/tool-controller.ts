/**
 * Generated by orval 🍺
 * Do not edit manually.
 * SSHOC Marketplace API
 * Social Sciences and Humanities Open Cloud Marketplace
 * OpenAPI spec version: 0.1.1
 */
import type {
  CreateToolParams,
  DeleteToolParams,
  GetToolAndVersionedItemDifferencesParams,
  GetToolHistoryParams,
  GetToolMergeParams,
  GetToolParams,
  GetToolsParams,
  GetVersionedToolAndVersionedItemDifferencesParams,
  ItemExtBasicDto,
  ItemsDifferencesDto,
  MergeToolParams,
  PaginatedTools,
  PatchToolParams,
  SourceDto,
  ToolCore,
  ToolDto,
  UpdateToolParams,
  UserDto,
} from '../models'

import { marketplaceFetch } from '../../../fetcher.ts'

export const getGetToolUrl = (persistentId: string, params?: GetToolParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/tools-services/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/tools-services/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Get single tool by its persistentId
 */
export const getTool = async (
  persistentId: string,
  params?: GetToolParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ToolDto> => {
  return marketplaceFetch<ToolDto>(getGetToolUrl(persistentId, params), {
    ...options,
    method: 'GET',
  })
}

export const getUpdateToolUrl = (
  persistentId: string,
  params?: UpdateToolParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/tools-services/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/tools-services/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Updating tool for given persistentId
 */
export const updateTool = async (
  persistentId: string,
  toolCore: ToolCore,
  params?: UpdateToolParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ToolDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<ToolDto>(getUpdateToolUrl(persistentId, params), {
    ...options,
    method: 'PUT',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(toolCore),
  })
}

export const getDeleteToolUrl = (
  persistentId: string,
  params?: DeleteToolParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/tools-services/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/tools-services/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Delete tool by its persistentId
 */
export const deleteTool = async (
  persistentId: string,
  params?: DeleteToolParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<void> => {
  return marketplaceFetch<void>(getDeleteToolUrl(persistentId, params), {
    ...options,
    method: 'DELETE',
  })
}

export const getPatchToolUrl = (
  persistentId: string,
  params?: PatchToolParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/tools-services/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/tools-services/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Patching a tool with new data for a given persistentId
 */
export const patchTool = async (
  persistentId: string,
  toolCore: ToolCore,
  params?: PatchToolParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ToolDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<ToolDto>(getPatchToolUrl(persistentId, params), {
    ...options,
    method: 'PATCH',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(toolCore),
  })
}

export const getRevertToolUrl = (persistentId: string, versionId: number) => {
  return `/api/tools-services/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/revert`
}

/**
 * @summary Revert tool to target version by its persistentId and versionId that is reverted to
 */
export const revertTool = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ToolDto> => {
  return marketplaceFetch<ToolDto>(getRevertToolUrl(persistentId, versionId), {
    ...options,
    method: 'PUT',
  })
}

export const getRevertTool1Url = (persistentId: string) => {
  return `/api/tools-services/${encodeURIComponent(String(persistentId))}/revert`
}

/**
 * @summary Revert tool by its persistentId. This method will set its status to ACTIVE and its versioned item to REVIEWED status and active state.
 */
export const revertTool1 = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ToolDto> => {
  return marketplaceFetch<ToolDto>(getRevertTool1Url(persistentId), {
    ...options,
    method: 'PUT',
  })
}

export const getGetToolsUrl = (params?: GetToolsParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/tools-services?${stringifiedParams}`
    : `/api/tools-services`
}

/**
 * @summary Retrieve all tools services in pages
 */
export const getTools = async (
  params?: GetToolsParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PaginatedTools> => {
  return marketplaceFetch<PaginatedTools>(getGetToolsUrl(params), {
    ...options,
    method: 'GET',
  })
}

export const getCreateToolUrl = (params?: CreateToolParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/tools-services?${stringifiedParams}`
    : `/api/tools-services`
}

/**
 * @summary Creating tool
 */
export const createTool = async (
  toolCore: ToolCore,
  params?: CreateToolParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ToolDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<ToolDto>(getCreateToolUrl(params), {
    ...options,
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(toolCore),
  })
}

export const getPublishToolUrl = (persistentId: string) => {
  return `/api/tools-services/${encodeURIComponent(String(persistentId))}/commit`
}

/**
 * @summary Committing draft of tool by its persistentId
 */
export const publishTool = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ToolDto> => {
  return marketplaceFetch<ToolDto>(getPublishToolUrl(persistentId), {
    ...options,
    method: 'POST',
  })
}

export const getMergeToolUrl = (params: MergeToolParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['with']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/tools-services/merge?${stringifiedParams}`
    : `/api/tools-services/merge`
}

/**
 * @summary Performing merge into tool
 */
export const mergeTool = async (
  toolCore: ToolCore,
  params: MergeToolParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ToolDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<ToolDto>(getMergeToolUrl(params), {
    ...options,
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(toolCore),
  })
}

export const getGetToolVersionUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/tools-services/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}`
}

/**
 * @summary Get tool selected version by its persistentId and versionId
 */
export const getToolVersion = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ToolDto> => {
  return marketplaceFetch<ToolDto>(
    getGetToolVersionUrl(persistentId, versionId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getDeleteToolVersionUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/tools-services/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}`
}

/**
 * @summary Delete tool by its persistentId and versionId
 */
export const deleteToolVersion = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<void> => {
  return marketplaceFetch<void>(
    getDeleteToolVersionUrl(persistentId, versionId),
    {
      ...options,
      method: 'DELETE',
    },
  )
}

export const getGetToolVersionInformationContributorsUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/tools-services/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/information-contributors`
}

/**
 * @summary Retrieving list of information-contributors to the selected version of tool by its persistentId and versionId
 */
export const getToolVersionInformationContributors = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<UserDto[]> => {
  return marketplaceFetch<UserDto[]>(
    getGetToolVersionInformationContributorsUrl(persistentId, versionId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetVersionedToolAndVersionedItemDifferencesUrl = (
  persistentId: string,
  versionId: number,
  params: GetVersionedToolAndVersionedItemDifferencesParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/tools-services/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/diff?${stringifiedParams}`
    : `/api/tools-services/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/diff`
}

/**
 * @summary Getting differences between target version of tool and target version of item ('unaltered' string response means for the single field that remained unchanged)
 */
export const getVersionedToolAndVersionedItemDifferences = async (
  persistentId: string,
  versionId: number,
  params: GetVersionedToolAndVersionedItemDifferencesParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemsDifferencesDto> => {
  return marketplaceFetch<ItemsDifferencesDto>(
    getGetVersionedToolAndVersionedItemDifferencesUrl(
      persistentId,
      versionId,
      params,
    ),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetToolSourcesUrl = (persistentId: string) => {
  return `/api/tools-services/${encodeURIComponent(String(persistentId))}/sources`
}

/**
 * @summary Getting list of sources of tool by its persistentId
 */
export const getToolSources = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<SourceDto[]> => {
  return marketplaceFetch<SourceDto[]>(getGetToolSourcesUrl(persistentId), {
    ...options,
    method: 'GET',
  })
}

export const getGetToolMergeUrl = (
  persistentId: string,
  params: GetToolMergeParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['with']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/tools-services/${encodeURIComponent(String(persistentId))}/merge?${stringifiedParams}`
    : `/api/tools-services/${encodeURIComponent(String(persistentId))}/merge`
}

/**
 * @summary Getting body of merged version of tool
 */
export const getToolMerge = async (
  persistentId: string,
  params: GetToolMergeParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ToolDto> => {
  return marketplaceFetch<ToolDto>(getGetToolMergeUrl(persistentId, params), {
    ...options,
    method: 'GET',
  })
}

export const getGetToolInformationContributorsUrl = (persistentId: string) => {
  return `/api/tools-services/${encodeURIComponent(String(persistentId))}/information-contributors`
}

/**
 * @summary Retrieving list of information-contributors across the whole history of tool by its persistentId
 */
export const getToolInformationContributors = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<UserDto[]> => {
  return marketplaceFetch<UserDto[]>(
    getGetToolInformationContributorsUrl(persistentId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetToolHistoryUrl = (
  persistentId: string,
  params?: GetToolHistoryParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/tools-services/${encodeURIComponent(String(persistentId))}/history?${stringifiedParams}`
    : `/api/tools-services/${encodeURIComponent(String(persistentId))}/history`
}

/**
 * @summary Retrieving history of tool by its persistentId
 */
export const getToolHistory = async (
  persistentId: string,
  params?: GetToolHistoryParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemExtBasicDto[]> => {
  return marketplaceFetch<ItemExtBasicDto[]>(
    getGetToolHistoryUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetToolAndVersionedItemDifferencesUrl = (
  persistentId: string,
  params: GetToolAndVersionedItemDifferencesParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/tools-services/${encodeURIComponent(String(persistentId))}/diff?${stringifiedParams}`
    : `/api/tools-services/${encodeURIComponent(String(persistentId))}/diff`
}

/**
 * @summary Getting differences between tool and target version of item ('unaltered' string response means for the single field that remained unchanged)
 */
export const getToolAndVersionedItemDifferences = async (
  persistentId: string,
  params: GetToolAndVersionedItemDifferencesParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemsDifferencesDto> => {
  return marketplaceFetch<ItemsDifferencesDto>(
    getGetToolAndVersionedItemDifferencesUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}
