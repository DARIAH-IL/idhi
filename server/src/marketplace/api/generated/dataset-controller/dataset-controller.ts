/**
 * Generated by orval 🍺
 * Do not edit manually.
 * SSHOC Marketplace API
 * Social Sciences and Humanities Open Cloud Marketplace
 * OpenAPI spec version: 0.1.1
 */
import type {
  CreateDatasetParams,
  DatasetCore,
  DatasetDto,
  DeleteDatasetParams,
  GetDatasetAndVersionedItemDifferencesParams,
  GetDatasetHistoryParams,
  GetDatasetMergeParams,
  GetDatasetParams,
  GetDatasetsParams,
  GetVersionedDatasetAndVersionedItemDifferencesParams,
  ItemExtBasicDto,
  ItemsDifferencesDto,
  MergeDatasetParams,
  PaginatedDatasets,
  PatchDatasetParams,
  SourceDto,
  UpdateDatasetParams,
  UserDto,
} from '../models'

import { marketplaceFetch } from '../../../fetcher.ts'

export const getGetDatasetUrl = (
  persistentId: string,
  params?: GetDatasetParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/datasets/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/datasets/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Get single dataset by its persistentId
 */
export const getDataset = async (
  persistentId: string,
  params?: GetDatasetParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<DatasetDto> => {
  return marketplaceFetch<DatasetDto>(getGetDatasetUrl(persistentId, params), {
    ...options,
    method: 'GET',
  })
}

export const getUpdateDatasetUrl = (
  persistentId: string,
  params?: UpdateDatasetParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/datasets/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/datasets/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Updating dataset for given persistentId
 */
export const updateDataset = async (
  persistentId: string,
  datasetCore: DatasetCore,
  params?: UpdateDatasetParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<DatasetDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<DatasetDto>(
    getUpdateDatasetUrl(persistentId, params),
    {
      ...options,
      method: 'PUT',
      headers: {
        'Content-Type': 'application/json',
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(datasetCore),
    },
  )
}

export const getDeleteDatasetUrl = (
  persistentId: string,
  params?: DeleteDatasetParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/datasets/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/datasets/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Delete dataset by its persistentId
 */
export const deleteDataset = async (
  persistentId: string,
  params?: DeleteDatasetParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<void> => {
  return marketplaceFetch<void>(getDeleteDatasetUrl(persistentId, params), {
    ...options,
    method: 'DELETE',
  })
}

export const getPatchDatasetUrl = (
  persistentId: string,
  params?: PatchDatasetParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/datasets/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/datasets/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Patch dataset for given persistentId
 */
export const patchDataset = async (
  persistentId: string,
  datasetCore: DatasetCore,
  params?: PatchDatasetParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<DatasetDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<DatasetDto>(
    getPatchDatasetUrl(persistentId, params),
    {
      ...options,
      method: 'PATCH',
      headers: {
        'Content-Type': 'application/json',
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(datasetCore),
    },
  )
}

export const getRevertDatasetUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/datasets/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/revert`
}

/**
 * @summary Revert dataset to target version by its persistentId and versionId that is reverted to
 */
export const revertDataset = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<DatasetDto> => {
  return marketplaceFetch<DatasetDto>(
    getRevertDatasetUrl(persistentId, versionId),
    {
      ...options,
      method: 'PUT',
    },
  )
}

export const getRevertDataset1Url = (persistentId: string) => {
  return `/api/datasets/${encodeURIComponent(String(persistentId))}/revert`
}

/**
 * @summary Revert dataset by its persistentId. This method will set its status to ACTIVE and its versioned item to REVIEWED status and active state.
 */
export const revertDataset1 = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<DatasetDto> => {
  return marketplaceFetch<DatasetDto>(getRevertDataset1Url(persistentId), {
    ...options,
    method: 'PUT',
  })
}

export const getGetDatasetsUrl = (params?: GetDatasetsParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/datasets?${stringifiedParams}`
    : `/api/datasets`
}

/**
 * @summary Retrieve all datasets in pages
 */
export const getDatasets = async (
  params?: GetDatasetsParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PaginatedDatasets> => {
  return marketplaceFetch<PaginatedDatasets>(getGetDatasetsUrl(params), {
    ...options,
    method: 'GET',
  })
}

export const getCreateDatasetUrl = (params?: CreateDatasetParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/datasets?${stringifiedParams}`
    : `/api/datasets`
}

/**
 * @summary Creating dataset
 */
export const createDataset = async (
  datasetCore: DatasetCore,
  params?: CreateDatasetParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<DatasetDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<DatasetDto>(getCreateDatasetUrl(params), {
    ...options,
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(datasetCore),
  })
}

export const getPublishDatasetUrl = (persistentId: string) => {
  return `/api/datasets/${encodeURIComponent(String(persistentId))}/commit`
}

/**
 * @summary Committing draft of dataset by its persistentId
 */
export const publishDataset = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<DatasetDto> => {
  return marketplaceFetch<DatasetDto>(getPublishDatasetUrl(persistentId), {
    ...options,
    method: 'POST',
  })
}

export const getMergeDatasetUrl = (params: MergeDatasetParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['with']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/datasets/merge?${stringifiedParams}`
    : `/api/datasets/merge`
}

/**
 * @summary Performing merge into dataset
 */
export const mergeDataset = async (
  datasetCore: DatasetCore,
  params: MergeDatasetParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<DatasetDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<DatasetDto>(getMergeDatasetUrl(params), {
    ...options,
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(datasetCore),
  })
}

export const getGetDatasetVersionUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/datasets/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}`
}

/**
 * @summary Get dataset selected version by its persistentId and versionId
 */
export const getDatasetVersion = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<DatasetDto> => {
  return marketplaceFetch<DatasetDto>(
    getGetDatasetVersionUrl(persistentId, versionId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getDeleteDatasetVersionUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/datasets/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}`
}

/**
 * @summary Delete dataset by its persistentId and versionId
 */
export const deleteDatasetVersion = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<void> => {
  return marketplaceFetch<void>(
    getDeleteDatasetVersionUrl(persistentId, versionId),
    {
      ...options,
      method: 'DELETE',
    },
  )
}

export const getGetDatasetVersionInformationContributorsUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/datasets/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/information-contributors`
}

/**
 * @summary Retrieving list of information-contributors to the selected version of dataset by its persistentId and versionId
 */
export const getDatasetVersionInformationContributors = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<UserDto[]> => {
  return marketplaceFetch<UserDto[]>(
    getGetDatasetVersionInformationContributorsUrl(persistentId, versionId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetVersionedDatasetAndVersionedItemDifferencesUrl = (
  persistentId: string,
  versionId: number,
  params: GetVersionedDatasetAndVersionedItemDifferencesParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/datasets/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/diff?${stringifiedParams}`
    : `/api/datasets/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/diff`
}

/**
 * @summary Getting differences between target version of dataset and target version of item ('unaltered' string response means for the single field that remained unchanged)
 */
export const getVersionedDatasetAndVersionedItemDifferences = async (
  persistentId: string,
  versionId: number,
  params: GetVersionedDatasetAndVersionedItemDifferencesParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemsDifferencesDto> => {
  return marketplaceFetch<ItemsDifferencesDto>(
    getGetVersionedDatasetAndVersionedItemDifferencesUrl(
      persistentId,
      versionId,
      params,
    ),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetDatasetSourcesUrl = (persistentId: string) => {
  return `/api/datasets/${encodeURIComponent(String(persistentId))}/sources`
}

/**
 * @summary Getting list of sources of dataset by its persistentId
 */
export const getDatasetSources = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<SourceDto[]> => {
  return marketplaceFetch<SourceDto[]>(getGetDatasetSourcesUrl(persistentId), {
    ...options,
    method: 'GET',
  })
}

export const getGetDatasetMergeUrl = (
  persistentId: string,
  params: GetDatasetMergeParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['with']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/datasets/${encodeURIComponent(String(persistentId))}/merge?${stringifiedParams}`
    : `/api/datasets/${encodeURIComponent(String(persistentId))}/merge`
}

/**
 * @summary Getting body of merged version of dataset
 */
export const getDatasetMerge = async (
  persistentId: string,
  params: GetDatasetMergeParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<DatasetDto> => {
  return marketplaceFetch<DatasetDto>(
    getGetDatasetMergeUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetDatasetInformationContributorsUrl = (
  persistentId: string,
) => {
  return `/api/datasets/${encodeURIComponent(String(persistentId))}/information-contributors`
}

/**
 * @summary Retrieving list of information-contributors across the whole history of dataset by its persistentId
 */
export const getDatasetInformationContributors = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<UserDto[]> => {
  return marketplaceFetch<UserDto[]>(
    getGetDatasetInformationContributorsUrl(persistentId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetDatasetHistoryUrl = (
  persistentId: string,
  params?: GetDatasetHistoryParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/datasets/${encodeURIComponent(String(persistentId))}/history?${stringifiedParams}`
    : `/api/datasets/${encodeURIComponent(String(persistentId))}/history`
}

/**
 * @summary Retrieving history of dataset by its persistentId
 */
export const getDatasetHistory = async (
  persistentId: string,
  params?: GetDatasetHistoryParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemExtBasicDto[]> => {
  return marketplaceFetch<ItemExtBasicDto[]>(
    getGetDatasetHistoryUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetDatasetAndVersionedItemDifferencesUrl = (
  persistentId: string,
  params: GetDatasetAndVersionedItemDifferencesParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/datasets/${encodeURIComponent(String(persistentId))}/diff?${stringifiedParams}`
    : `/api/datasets/${encodeURIComponent(String(persistentId))}/diff`
}

/**
 * @summary Getting differences between dataset and target version of item, ('unaltered' string response means for the single field that remained unchanged)
 */
export const getDatasetAndVersionedItemDifferences = async (
  persistentId: string,
  params: GetDatasetAndVersionedItemDifferencesParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemsDifferencesDto> => {
  return marketplaceFetch<ItemsDifferencesDto>(
    getGetDatasetAndVersionedItemDifferencesUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}
