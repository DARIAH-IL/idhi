/**
 * Generated by orval 🍺
 * Do not edit manually.
 * SSHOC Marketplace API
 * Social Sciences and Humanities Open Cloud Marketplace
 * OpenAPI spec version: 0.1.1
 */
import type {
  GetItemsForSourceAndSourceItemIdParams,
  GetItemsForSourceParams,
  GetSourcesParams,
  PaginatedItemsBasicItemBasicDto,
  PaginatedSources,
  SourceCore,
  SourceDto,
} from '../models'

import { marketplaceFetch } from '../../../fetcher.ts'

export const getGetSourceUrl = (id: number) => {
  return `/api/sources/${encodeURIComponent(String(id))}`
}

export const getSource = async (
  id: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<SourceDto> => {
  return marketplaceFetch<SourceDto>(getGetSourceUrl(id), {
    ...options,
    method: 'GET',
  })
}

export const getUpdateSourceUrl = (id: number) => {
  return `/api/sources/${encodeURIComponent(String(id))}`
}

export const updateSource = async (
  id: number,
  sourceCore: SourceCore,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<SourceDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<SourceDto>(getUpdateSourceUrl(id), {
    ...options,
    method: 'PUT',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(sourceCore),
  })
}

export const getDeleteSourceUrl = (id: number) => {
  return `/api/sources/${encodeURIComponent(String(id))}`
}

export const deleteSource = async (
  id: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<void> => {
  return marketplaceFetch<void>(getDeleteSourceUrl(id), {
    ...options,
    method: 'DELETE',
  })
}

export const getGetSourcesUrl = (params?: GetSourcesParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/sources?${stringifiedParams}`
    : `/api/sources`
}

export const getSources = async (
  params?: GetSourcesParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PaginatedSources> => {
  return marketplaceFetch<PaginatedSources>(getGetSourcesUrl(params), {
    ...options,
    method: 'GET',
  })
}

export const getCreateSourceUrl = () => {
  return `/api/sources`
}

export const createSource = async (
  sourceCore: SourceCore,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<SourceDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<SourceDto>(getCreateSourceUrl(), {
    ...options,
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(sourceCore),
  })
}

export const getGetItemsForSourceUrl = (
  sourceId: number,
  params?: GetItemsForSourceParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/sources/${encodeURIComponent(String(sourceId))}/items?${stringifiedParams}`
    : `/api/sources/${encodeURIComponent(String(sourceId))}/items`
}

/**
 * @summary Get list of items for given source
 */
export const getItemsForSource = async (
  sourceId: number,
  params?: GetItemsForSourceParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PaginatedItemsBasicItemBasicDto> => {
  return marketplaceFetch<PaginatedItemsBasicItemBasicDto>(
    getGetItemsForSourceUrl(sourceId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetItemsForSourceAndSourceItemIdUrl = (
  sourceId: number,
  sourceItemId: string,
  params?: GetItemsForSourceAndSourceItemIdParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/sources/${encodeURIComponent(String(sourceId))}/items/${encodeURIComponent(String(sourceItemId))}?${stringifiedParams}`
    : `/api/sources/${encodeURIComponent(String(sourceId))}/items/${encodeURIComponent(String(sourceItemId))}`
}

/**
 * @summary Get list of items for given source and id of an item in this source
 */
export const getItemsForSourceAndSourceItemId = async (
  sourceId: number,
  sourceItemId: string,
  params?: GetItemsForSourceAndSourceItemIdParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PaginatedItemsBasicItemBasicDto> => {
  return marketplaceFetch<PaginatedItemsBasicItemBasicDto>(
    getGetItemsForSourceAndSourceItemIdUrl(sourceId, sourceItemId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}
