/**
 * Generated by orval 🍺
 * Do not edit manually.
 * SSHOC Marketplace API
 * Social Sciences and Humanities Open Cloud Marketplace
 * OpenAPI spec version: 0.1.1
 */
import type {
  ActorCore,
  ActorDto,
  ActorHistoryDto,
  DeleteActorParams,
  GetActorParams,
  GetActorsParams,
  MergeActorParams,
  PaginatedActors,
} from '../models'

import { marketplaceFetch } from '../../../fetcher.ts'

export const getGetActorUrl = (id: number, params?: GetActorParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/actors/${encodeURIComponent(String(id))}?${stringifiedParams}`
    : `/api/actors/${encodeURIComponent(String(id))}`
}

/**
 * @summary Get actor given by id with optional list of items that actor contributes to
 */
export const getActor = async (
  id: number,
  params?: GetActorParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ActorDto> => {
  return marketplaceFetch<ActorDto>(getGetActorUrl(id, params), {
    ...options,
    method: 'GET',
  })
}

export const getUpdateActorUrl = (id: number) => {
  return `/api/actors/${encodeURIComponent(String(id))}`
}

export const updateActor = async (
  id: number,
  actorCore: ActorCore,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ActorDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<ActorDto>(getUpdateActorUrl(id), {
    ...options,
    method: 'PUT',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(actorCore),
  })
}

export const getDeleteActorUrl = (id: number, params?: DeleteActorParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/actors/${encodeURIComponent(String(id))}?${stringifiedParams}`
    : `/api/actors/${encodeURIComponent(String(id))}`
}

/**
 * @summary Delete actor by given id. Force delete can be used by administrators only.
 */
export const deleteActor = async (
  id: number,
  params?: DeleteActorParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<void> => {
  return marketplaceFetch<void>(getDeleteActorUrl(id, params), {
    ...options,
    method: 'DELETE',
  })
}

export const getGetActorsUrl = (params?: GetActorsParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/actors?${stringifiedParams}`
    : `/api/actors`
}

/**
 * @summary Get list of actors in pages
 */
export const getActors = async (
  params?: GetActorsParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PaginatedActors> => {
  return marketplaceFetch<PaginatedActors>(getGetActorsUrl(params), {
    ...options,
    method: 'GET',
  })
}

export const getCreateActorUrl = () => {
  return `/api/actors`
}

export const createActor = async (
  actorCore: ActorCore,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ActorDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<ActorDto>(getCreateActorUrl(), {
    ...options,
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(actorCore),
  })
}

export const getMergeActorUrl = (id: number, params: MergeActorParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['with']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/actors/${encodeURIComponent(String(id))}/merge?${stringifiedParams}`
    : `/api/actors/${encodeURIComponent(String(id))}/merge`
}

/**
 * @summary Merge of actors
 */
export const mergeActor = async (
  id: number,
  params: MergeActorParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ActorDto> => {
  return marketplaceFetch<ActorDto>(getMergeActorUrl(id, params), {
    ...options,
    method: 'POST',
  })
}

export const getGetActorHistoryUrl = (id: number) => {
  return `/api/actors/${encodeURIComponent(String(id))}/history`
}

/**
 * @summary History of actor
 */
export const getActorHistory = async (
  id: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ActorHistoryDto[]> => {
  return marketplaceFetch<ActorHistoryDto[]>(getGetActorHistoryUrl(id), {
    ...options,
    method: 'GET',
  })
}
