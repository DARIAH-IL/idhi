/**
 * Generated by orval 🍺
 * Do not edit manually.
 * SSHOC Marketplace API
 * Social Sciences and Humanities Open Cloud Marketplace
 * OpenAPI spec version: 0.1.1
 */
import type {
  CreatePublicationParams,
  DeletePublicationParams,
  GetPublicationAndVersionedItemDifferencesParams,
  GetPublicationHistoryParams,
  GetPublicationMergeParams,
  GetPublicationParams,
  GetPublicationsParams,
  GetVersionedPublicationAndVersionedItemDifferencesParams,
  ItemExtBasicDto,
  ItemsDifferencesDto,
  MergePublicationParams,
  PaginatedPublications,
  PatchPublicationParams,
  PublicationCore,
  PublicationDto,
  SourceDto,
  UpdatePublicationParams,
  UserDto,
} from '../models'

import { marketplaceFetch } from '../../../fetcher.ts'

export const getGetPublicationUrl = (
  persistentId: string,
  params?: GetPublicationParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/publications/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/publications/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Get single publication by its persistentId
 */
export const getPublication = async (
  persistentId: string,
  params?: GetPublicationParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PublicationDto> => {
  return marketplaceFetch<PublicationDto>(
    getGetPublicationUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getUpdatePublicationUrl = (
  persistentId: string,
  params?: UpdatePublicationParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/publications/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/publications/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Updating publication for given persistentId
 */
export const updatePublication = async (
  persistentId: string,
  publicationCore: PublicationCore,
  params?: UpdatePublicationParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PublicationDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<PublicationDto>(
    getUpdatePublicationUrl(persistentId, params),
    {
      ...options,
      method: 'PUT',
      headers: {
        'Content-Type': 'application/json',
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(publicationCore),
    },
  )
}

export const getDeletePublicationUrl = (
  persistentId: string,
  params?: DeletePublicationParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/publications/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/publications/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Delete publication by its persistentId
 */
export const deletePublication = async (
  persistentId: string,
  params?: DeletePublicationParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<void> => {
  return marketplaceFetch<void>(getDeletePublicationUrl(persistentId, params), {
    ...options,
    method: 'DELETE',
  })
}

export const getPatchPublicationUrl = (
  persistentId: string,
  params?: PatchPublicationParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/publications/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/publications/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Patch publication for given persistentId
 */
export const patchPublication = async (
  persistentId: string,
  publicationCore: PublicationCore,
  params?: PatchPublicationParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PublicationDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<PublicationDto>(
    getPatchPublicationUrl(persistentId, params),
    {
      ...options,
      method: 'PATCH',
      headers: {
        'Content-Type': 'application/json',
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(publicationCore),
    },
  )
}

export const getRevertPublicationUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/publications/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/revert`
}

/**
 * @summary Revert publication to target version by its persistentId and versionId that is reverted to
 */
export const revertPublication = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PublicationDto> => {
  return marketplaceFetch<PublicationDto>(
    getRevertPublicationUrl(persistentId, versionId),
    {
      ...options,
      method: 'PUT',
    },
  )
}

export const getRevertPublication1Url = (persistentId: string) => {
  return `/api/publications/${encodeURIComponent(String(persistentId))}/revert`
}

/**
 * @summary Revert publication by its persistentId. This method will set its status to ACTIVE and its versioned item to REVIEWED status and active state.
 */
export const revertPublication1 = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PublicationDto> => {
  return marketplaceFetch<PublicationDto>(
    getRevertPublication1Url(persistentId),
    {
      ...options,
      method: 'PUT',
    },
  )
}

export const getGetPublicationsUrl = (params?: GetPublicationsParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/publications?${stringifiedParams}`
    : `/api/publications`
}

/**
 * @summary Retrieve all publications in pages
 */
export const getPublications = async (
  params?: GetPublicationsParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PaginatedPublications> => {
  return marketplaceFetch<PaginatedPublications>(
    getGetPublicationsUrl(params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getCreatePublicationUrl = (params?: CreatePublicationParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/publications?${stringifiedParams}`
    : `/api/publications`
}

/**
 * @summary Creating publication
 */
export const createPublication = async (
  publicationCore: PublicationCore,
  params?: CreatePublicationParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PublicationDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<PublicationDto>(getCreatePublicationUrl(params), {
    ...options,
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(publicationCore),
  })
}

export const getPublishPublicationUrl = (persistentId: string) => {
  return `/api/publications/${encodeURIComponent(String(persistentId))}/commit`
}

/**
 * @summary Committing draft of publication by its persistentId
 */
export const publishPublication = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PublicationDto> => {
  return marketplaceFetch<PublicationDto>(
    getPublishPublicationUrl(persistentId),
    {
      ...options,
      method: 'POST',
    },
  )
}

export const getMergePublicationUrl = (params: MergePublicationParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['with']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/publications/merge?${stringifiedParams}`
    : `/api/publications/merge`
}

/**
 * @summary Performing merged into publication
 */
export const mergePublication = async (
  publicationCore: PublicationCore,
  params: MergePublicationParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PublicationDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<PublicationDto>(getMergePublicationUrl(params), {
    ...options,
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(publicationCore),
  })
}

export const getGetPublicationVersionUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/publications/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}`
}

/**
 * @summary Get publication selected version by its persistentId and versionId
 */
export const getPublicationVersion = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PublicationDto> => {
  return marketplaceFetch<PublicationDto>(
    getGetPublicationVersionUrl(persistentId, versionId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getDeletePublicationVersionUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/publications/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}`
}

/**
 * @summary Delete publication by its persistentId and versionId
 */
export const deletePublicationVersion = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<void> => {
  return marketplaceFetch<void>(
    getDeletePublicationVersionUrl(persistentId, versionId),
    {
      ...options,
      method: 'DELETE',
    },
  )
}

export const getGetPublicationVersionInformationContributorsUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/publications/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/information-contributors`
}

/**
 * @summary Retrieving list of information-contributors to the selected version of publication by its persistentId and versionId
 */
export const getPublicationVersionInformationContributors = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<UserDto[]> => {
  return marketplaceFetch<UserDto[]>(
    getGetPublicationVersionInformationContributorsUrl(persistentId, versionId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetVersionedPublicationAndVersionedItemDifferencesUrl = (
  persistentId: string,
  versionId: number,
  params: GetVersionedPublicationAndVersionedItemDifferencesParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/publications/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/diff?${stringifiedParams}`
    : `/api/publications/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/diff`
}

/**
 * @summary Getting differences between target version of publication and target version of item ('unaltered' string response means for the single field that remained unchanged)
 */
export const getVersionedPublicationAndVersionedItemDifferences = async (
  persistentId: string,
  versionId: number,
  params: GetVersionedPublicationAndVersionedItemDifferencesParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemsDifferencesDto> => {
  return marketplaceFetch<ItemsDifferencesDto>(
    getGetVersionedPublicationAndVersionedItemDifferencesUrl(
      persistentId,
      versionId,
      params,
    ),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetPublicationSourcesUrl = (persistentId: string) => {
  return `/api/publications/${encodeURIComponent(String(persistentId))}/sources`
}

/**
 * @summary Getting list of sources of publication by its persistentId
 */
export const getPublicationSources = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<SourceDto[]> => {
  return marketplaceFetch<SourceDto[]>(
    getGetPublicationSourcesUrl(persistentId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetPublicationMergeUrl = (
  persistentId: string,
  params: GetPublicationMergeParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['with']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/publications/${encodeURIComponent(String(persistentId))}/merge?${stringifiedParams}`
    : `/api/publications/${encodeURIComponent(String(persistentId))}/merge`
}

/**
 * @summary Getting body of merged version of publication
 */
export const getPublicationMerge = async (
  persistentId: string,
  params: GetPublicationMergeParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PublicationDto> => {
  return marketplaceFetch<PublicationDto>(
    getGetPublicationMergeUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetPublicationInformationContributorsUrl = (
  persistentId: string,
) => {
  return `/api/publications/${encodeURIComponent(String(persistentId))}/information-contributors`
}

/**
 * @summary Retrieving list of information-contributors across the whole history of publication by its persistentId
 */
export const getPublicationInformationContributors = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<UserDto[]> => {
  return marketplaceFetch<UserDto[]>(
    getGetPublicationInformationContributorsUrl(persistentId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetPublicationHistoryUrl = (
  persistentId: string,
  params?: GetPublicationHistoryParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/publications/${encodeURIComponent(String(persistentId))}/history?${stringifiedParams}`
    : `/api/publications/${encodeURIComponent(String(persistentId))}/history`
}

/**
 * @summary Retrieving history of publication by its persistentId
 */
export const getPublicationHistory = async (
  persistentId: string,
  params?: GetPublicationHistoryParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemExtBasicDto[]> => {
  return marketplaceFetch<ItemExtBasicDto[]>(
    getGetPublicationHistoryUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetPublicationAndVersionedItemDifferencesUrl = (
  persistentId: string,
  params: GetPublicationAndVersionedItemDifferencesParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/publications/${encodeURIComponent(String(persistentId))}/diff?${stringifiedParams}`
    : `/api/publications/${encodeURIComponent(String(persistentId))}/diff`
}

/**
 * @summary Getting differences between publication and target version of item ('unaltered' string response means for the single field that remained unchanged)
 */
export const getPublicationAndVersionedItemDifferences = async (
  persistentId: string,
  params: GetPublicationAndVersionedItemDifferencesParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemsDifferencesDto> => {
  return marketplaceFetch<ItemsDifferencesDto>(
    getGetPublicationAndVersionedItemDifferencesUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}
