/**
 * Generated by orval 🍺
 * Do not edit manually.
 * SSHOC Marketplace API
 * Social Sciences and Humanities Open Cloud Marketplace
 * OpenAPI spec version: 0.1.1
 */
import type {
  AutocompleteItemsParams,
  PaginatedSearchActor,
  PaginatedSearchConcepts,
  PaginatedSearchItems,
  SearchActorsParams,
  SearchConceptsParams,
  SearchItemsParams,
  SuggestedSearchPhrases,
} from '../models'

import { marketplaceFetch } from '../../../fetcher.ts'

export const getSearchItemsUrl = (params?: SearchItemsParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['categories', 'order']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/item-search?${stringifiedParams}`
    : `/api/item-search`
}

/**
 * Search among items.
 */
export const searchItems = async (
  params?: SearchItemsParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PaginatedSearchItems> => {
  return marketplaceFetch<PaginatedSearchItems>(getSearchItemsUrl(params), {
    ...options,
    method: 'GET',
  })
}

export const getAutocompleteItemsUrl = (params: AutocompleteItemsParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/item-search/autocomplete?${stringifiedParams}`
    : `/api/item-search/autocomplete`
}

/**
 * Autocomplete for items search.
 */
export const autocompleteItems = async (
  params: AutocompleteItemsParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<SuggestedSearchPhrases> => {
  return marketplaceFetch<SuggestedSearchPhrases>(
    getAutocompleteItemsUrl(params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getSearchConceptsUrl = (params?: SearchConceptsParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['types']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/concept-search?${stringifiedParams}`
    : `/api/concept-search`
}

/**
 * Search among concepts.
 */
export const searchConcepts = async (
  params?: SearchConceptsParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PaginatedSearchConcepts> => {
  return marketplaceFetch<PaginatedSearchConcepts>(
    getSearchConceptsUrl(params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getSearchActorsUrl = (params?: SearchActorsParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/actor-search?${stringifiedParams}`
    : `/api/actor-search`
}

/**
 * Search among actors.
 */
export const searchActors = async (
  params?: SearchActorsParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PaginatedSearchActor> => {
  return marketplaceFetch<PaginatedSearchActor>(getSearchActorsUrl(params), {
    ...options,
    method: 'GET',
  })
}
