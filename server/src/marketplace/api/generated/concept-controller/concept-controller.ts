/**
 * Generated by orval 🍺
 * Do not edit manually.
 * SSHOC Marketplace API
 * Social Sciences and Humanities Open Cloud Marketplace
 * OpenAPI spec version: 0.1.1
 */
import type {
  ConceptCore,
  ConceptDto,
  CreateConceptParams,
  DeleteConceptParams,
  MergeConceptsParams,
} from '../models'

import { marketplaceFetch } from '../../../fetcher.ts'

export const getGetConceptUrl = (vocabularyCode: string, code: string) => {
  return `/api/vocabularies/${encodeURIComponent(String(vocabularyCode))}/concepts/${encodeURIComponent(String(code))}`
}

/**
 * @summary Get concept for given vocabulary code and concept code
 */
export const getConcept = async (
  vocabularyCode: string,
  code: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ConceptDto> => {
  return marketplaceFetch<ConceptDto>(getGetConceptUrl(vocabularyCode, code), {
    ...options,
    method: 'GET',
  })
}

export const getUpdateConceptUrl = (vocabularyCode: string, code: string) => {
  return `/api/vocabularies/${encodeURIComponent(String(vocabularyCode))}/concepts/${encodeURIComponent(String(code))}`
}

/**
 * @summary Update concept for given vocabulary code and concept code
 */
export const updateConcept = async (
  vocabularyCode: string,
  code: string,
  conceptCore: ConceptCore,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ConceptDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<ConceptDto>(
    getUpdateConceptUrl(vocabularyCode, code),
    {
      ...options,
      method: 'PUT',
      headers: {
        'Content-Type': 'application/json',
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(conceptCore),
    },
  )
}

export const getDeleteConceptUrl = (
  vocabularyCode: string,
  code: string,
  params?: DeleteConceptParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/vocabularies/${encodeURIComponent(String(vocabularyCode))}/concepts/${encodeURIComponent(String(code))}?${stringifiedParams}`
    : `/api/vocabularies/${encodeURIComponent(String(vocabularyCode))}/concepts/${encodeURIComponent(String(code))}`
}

/**
 * @summary Delete concept for given vocabulary code and concept code
 */
export const deleteConcept = async (
  vocabularyCode: string,
  code: string,
  params?: DeleteConceptParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<void> => {
  return marketplaceFetch<void>(
    getDeleteConceptUrl(vocabularyCode, code, params),
    {
      ...options,
      method: 'DELETE',
    },
  )
}

export const getCommitConceptUrl = (vocabularyCode: string, code: string) => {
  return `/api/vocabularies/${encodeURIComponent(String(vocabularyCode))}/concepts/${encodeURIComponent(String(code))}/commit`
}

/**
 * @summary Commit concept for given vocabulary code and concept code
 */
export const commitConcept = async (
  vocabularyCode: string,
  code: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ConceptDto> => {
  return marketplaceFetch<ConceptDto>(
    getCommitConceptUrl(vocabularyCode, code),
    {
      ...options,
      method: 'PUT',
    },
  )
}

export const getCreateConceptUrl = (
  vocabularyCode: string,
  params?: CreateConceptParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/vocabularies/${encodeURIComponent(String(vocabularyCode))}/concepts?${stringifiedParams}`
    : `/api/vocabularies/${encodeURIComponent(String(vocabularyCode))}/concepts`
}

/**
 * @summary Create concept for given vocabulary code
 */
export const createConcept = async (
  vocabularyCode: string,
  conceptCore: ConceptCore,
  params?: CreateConceptParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ConceptDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<ConceptDto>(
    getCreateConceptUrl(vocabularyCode, params),
    {
      ...options,
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(conceptCore),
    },
  )
}

export const getMergeConceptsUrl = (
  vocabularyCode: string,
  code: string,
  params: MergeConceptsParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['with']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/vocabularies/${encodeURIComponent(String(vocabularyCode))}/concepts/${encodeURIComponent(String(code))}/merge?${stringifiedParams}`
    : `/api/vocabularies/${encodeURIComponent(String(vocabularyCode))}/concepts/${encodeURIComponent(String(code))}/merge`
}

/**
 * @summary Merge concepts
 */
export const mergeConcepts = async (
  vocabularyCode: string,
  code: string,
  params: MergeConceptsParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ConceptDto> => {
  return marketplaceFetch<ConceptDto>(
    getMergeConceptsUrl(vocabularyCode, code, params),
    {
      ...options,
      method: 'POST',
    },
  )
}
