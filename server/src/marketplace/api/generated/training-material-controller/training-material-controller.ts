/**
 * Generated by orval 🍺
 * Do not edit manually.
 * SSHOC Marketplace API
 * Social Sciences and Humanities Open Cloud Marketplace
 * OpenAPI spec version: 0.1.1
 */
import type {
  CreateTrainingMaterialParams,
  DeleteTrainingMaterialParams,
  GetTrainingMaterialAndVersionedItemDifferencesParams,
  GetTrainingMaterialHistoryParams,
  GetTrainingMaterialMergeParams,
  GetTrainingMaterialParams,
  GetTrainingMaterialsParams,
  GetVersionedTrainingMaterialAndVersionedItemDifferencesParams,
  ItemExtBasicDto,
  ItemsDifferencesDto,
  MergeTrainingMaterialParams,
  PaginatedTrainingMaterials,
  PatchTrainingMaterialParams,
  SourceDto,
  TrainingMaterialCore,
  TrainingMaterialDto,
  UpdateTrainingMaterialParams,
  UserDto,
} from '../models'

import { marketplaceFetch } from '../../../fetcher.ts'

export const getGetTrainingMaterialUrl = (
  persistentId: string,
  params?: GetTrainingMaterialParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/training-materials/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/training-materials/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Get single training material by its persistentId
 */
export const getTrainingMaterial = async (
  persistentId: string,
  params?: GetTrainingMaterialParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<TrainingMaterialDto> => {
  return marketplaceFetch<TrainingMaterialDto>(
    getGetTrainingMaterialUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getUpdateTrainingMaterialUrl = (
  persistentId: string,
  params?: UpdateTrainingMaterialParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/training-materials/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/training-materials/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Updating training material for given persistentId
 */
export const updateTrainingMaterial = async (
  persistentId: string,
  trainingMaterialCore: TrainingMaterialCore,
  params?: UpdateTrainingMaterialParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<TrainingMaterialDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<TrainingMaterialDto>(
    getUpdateTrainingMaterialUrl(persistentId, params),
    {
      ...options,
      method: 'PUT',
      headers: {
        'Content-Type': 'application/json',
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(trainingMaterialCore),
    },
  )
}

export const getDeleteTrainingMaterialUrl = (
  persistentId: string,
  params?: DeleteTrainingMaterialParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/training-materials/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/training-materials/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Delete training material by its persistentId
 */
export const deleteTrainingMaterial = async (
  persistentId: string,
  params?: DeleteTrainingMaterialParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<void> => {
  return marketplaceFetch<void>(
    getDeleteTrainingMaterialUrl(persistentId, params),
    {
      ...options,
      method: 'DELETE',
    },
  )
}

export const getPatchTrainingMaterialUrl = (
  persistentId: string,
  params?: PatchTrainingMaterialParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/training-materials/${encodeURIComponent(String(persistentId))}?${stringifiedParams}`
    : `/api/training-materials/${encodeURIComponent(String(persistentId))}`
}

/**
 * @summary Patch training material for given persistentId
 */
export const patchTrainingMaterial = async (
  persistentId: string,
  trainingMaterialCore: TrainingMaterialCore,
  params?: PatchTrainingMaterialParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<TrainingMaterialDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<TrainingMaterialDto>(
    getPatchTrainingMaterialUrl(persistentId, params),
    {
      ...options,
      method: 'PATCH',
      headers: {
        'Content-Type': 'application/json',
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(trainingMaterialCore),
    },
  )
}

export const getRevertTrainingMaterialUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/training-materials/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/revert`
}

/**
 * @summary Revert training material to target version by its persistentId and versionId that is reverted to
 */
export const revertTrainingMaterial = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<TrainingMaterialDto> => {
  return marketplaceFetch<TrainingMaterialDto>(
    getRevertTrainingMaterialUrl(persistentId, versionId),
    {
      ...options,
      method: 'PUT',
    },
  )
}

export const getRevertTrainingMaterial1Url = (persistentId: string) => {
  return `/api/training-materials/${encodeURIComponent(String(persistentId))}/revert`
}

/**
 * @summary Revert training material by its persistentId. This method will set its status to ACTIVE and its versioned item to REVIEWED status and active state.
 */
export const revertTrainingMaterial1 = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<TrainingMaterialDto> => {
  return marketplaceFetch<TrainingMaterialDto>(
    getRevertTrainingMaterial1Url(persistentId),
    {
      ...options,
      method: 'PUT',
    },
  )
}

export const getGetTrainingMaterialsUrl = (
  params?: GetTrainingMaterialsParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/training-materials?${stringifiedParams}`
    : `/api/training-materials`
}

/**
 * @summary Retrieve all training materials in pages
 */
export const getTrainingMaterials = async (
  params?: GetTrainingMaterialsParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<PaginatedTrainingMaterials> => {
  return marketplaceFetch<PaginatedTrainingMaterials>(
    getGetTrainingMaterialsUrl(params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getCreateTrainingMaterialUrl = (
  params?: CreateTrainingMaterialParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/training-materials?${stringifiedParams}`
    : `/api/training-materials`
}

/**
 * @summary Creating training material
 */
export const createTrainingMaterial = async (
  trainingMaterialCore: TrainingMaterialCore,
  params?: CreateTrainingMaterialParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<TrainingMaterialDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<TrainingMaterialDto>(
    getCreateTrainingMaterialUrl(params),
    {
      ...options,
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(trainingMaterialCore),
    },
  )
}

export const getPublishTrainingMaterialUrl = (persistentId: string) => {
  return `/api/training-materials/${encodeURIComponent(String(persistentId))}/commit`
}

/**
 * @summary Committing draft of training material by its persistentId
 */
export const publishTrainingMaterial = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<TrainingMaterialDto> => {
  return marketplaceFetch<TrainingMaterialDto>(
    getPublishTrainingMaterialUrl(persistentId),
    {
      ...options,
      method: 'POST',
    },
  )
}

export const getMergeTrainingMaterialUrl = (
  params: MergeTrainingMaterialParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['with']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/training-materials/merge?${stringifiedParams}`
    : `/api/training-materials/merge`
}

/**
 * @summary Performing merge into training material
 */
export const mergeTrainingMaterial = async (
  trainingMaterialCore: TrainingMaterialCore,
  params: MergeTrainingMaterialParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<TrainingMaterialDto> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) {
      return {}
    }
    if (h instanceof Headers) {
      return Object.fromEntries(h.entries())
    }
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) {
        headers[name] = value
      }
    }
    return headers
  }
  return marketplaceFetch<TrainingMaterialDto>(
    getMergeTrainingMaterialUrl(params),
    {
      ...options,
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        ...getHeaders(options?.headers),
      },
      body: JSON.stringify(trainingMaterialCore),
    },
  )
}

export const getGetTrainingMaterialVersionUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/training-materials/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}`
}

/**
 * @summary Get training material selected version by its persistentId and versionId
 */
export const getTrainingMaterialVersion = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<TrainingMaterialDto> => {
  return marketplaceFetch<TrainingMaterialDto>(
    getGetTrainingMaterialVersionUrl(persistentId, versionId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getDeleteTrainingMaterialVersionUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/training-materials/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}`
}

/**
 * @summary Delete training material by its persistentId and versionId
 */
export const deleteTrainingMaterialVersion = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<void> => {
  return marketplaceFetch<void>(
    getDeleteTrainingMaterialVersionUrl(persistentId, versionId),
    {
      ...options,
      method: 'DELETE',
    },
  )
}

export const getGetTrainingMaterialVersionInformationContributorsUrl = (
  persistentId: string,
  versionId: number,
) => {
  return `/api/training-materials/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/information-contributors`
}

/**
 * @summary Retrieving list of information-contributors to the selected version of training material by its persistentId and versionId
 */
export const getTrainingMaterialVersionInformationContributors = async (
  persistentId: string,
  versionId: number,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<UserDto[]> => {
  return marketplaceFetch<UserDto[]>(
    getGetTrainingMaterialVersionInformationContributorsUrl(
      persistentId,
      versionId,
    ),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetVersionedTrainingMaterialAndVersionedItemDifferencesUrl = (
  persistentId: string,
  versionId: number,
  params: GetVersionedTrainingMaterialAndVersionedItemDifferencesParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/training-materials/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/diff?${stringifiedParams}`
    : `/api/training-materials/${encodeURIComponent(String(persistentId))}/versions/${encodeURIComponent(String(versionId))}/diff`
}

/**
 * @summary Getting differences between target version of training material and target version of item ('unaltered' string response means for the single field that remained unchanged)
 */
export const getVersionedTrainingMaterialAndVersionedItemDifferences = async (
  persistentId: string,
  versionId: number,
  params: GetVersionedTrainingMaterialAndVersionedItemDifferencesParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemsDifferencesDto> => {
  return marketplaceFetch<ItemsDifferencesDto>(
    getGetVersionedTrainingMaterialAndVersionedItemDifferencesUrl(
      persistentId,
      versionId,
      params,
    ),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetTrainingMaterialSourcesUrl = (persistentId: string) => {
  return `/api/training-materials/${encodeURIComponent(String(persistentId))}/sources`
}

/**
 * @summary Getting list of sources of training material by its persistentId
 */
export const getTrainingMaterialSources = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<SourceDto[]> => {
  return marketplaceFetch<SourceDto[]>(
    getGetTrainingMaterialSourcesUrl(persistentId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetTrainingMaterialMergeUrl = (
  persistentId: string,
  params: GetTrainingMaterialMergeParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['with']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : String(v))
      })
      return
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/training-materials/${encodeURIComponent(String(persistentId))}/merge?${stringifiedParams}`
    : `/api/training-materials/${encodeURIComponent(String(persistentId))}/merge`
}

/**
 * @summary Getting body of merged version of training material
 */
export const getTrainingMaterialMerge = async (
  persistentId: string,
  params: GetTrainingMaterialMergeParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<TrainingMaterialDto> => {
  return marketplaceFetch<TrainingMaterialDto>(
    getGetTrainingMaterialMergeUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetTrainingMaterialInformationContributorsUrl = (
  persistentId: string,
) => {
  return `/api/training-materials/${encodeURIComponent(String(persistentId))}/information-contributors`
}

/**
 * @summary Retrieving list of information-contributors across the whole history of training material by its persistentId
 */
export const getTrainingMaterialInformationContributors = async (
  persistentId: string,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<UserDto[]> => {
  return marketplaceFetch<UserDto[]>(
    getGetTrainingMaterialInformationContributorsUrl(persistentId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetTrainingMaterialHistoryUrl = (
  persistentId: string,
  params?: GetTrainingMaterialHistoryParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/training-materials/${encodeURIComponent(String(persistentId))}/history?${stringifiedParams}`
    : `/api/training-materials/${encodeURIComponent(String(persistentId))}/history`
}

/**
 * @summary Retrieving history of training material by its persistentId
 */
export const getTrainingMaterialHistory = async (
  persistentId: string,
  params?: GetTrainingMaterialHistoryParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemExtBasicDto[]> => {
  return marketplaceFetch<ItemExtBasicDto[]>(
    getGetTrainingMaterialHistoryUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetTrainingMaterialAndVersionedItemDifferencesUrl = (
  persistentId: string,
  params: GetTrainingMaterialAndVersionedItemDifferencesParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/training-materials/${encodeURIComponent(String(persistentId))}/diff?${stringifiedParams}`
    : `/api/training-materials/${encodeURIComponent(String(persistentId))}/diff`
}

/**
 * @summary Getting differences between training material and target version of item ('unaltered' string response means for the single field that remained unchanged)
 */
export const getTrainingMaterialAndVersionedItemDifferences = async (
  persistentId: string,
  params: GetTrainingMaterialAndVersionedItemDifferencesParams,
  options?: Parameters<typeof marketplaceFetch>[1],
): Promise<ItemsDifferencesDto> => {
  return marketplaceFetch<ItemsDifferencesDto>(
    getGetTrainingMaterialAndVersionedItemDifferencesUrl(persistentId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}
